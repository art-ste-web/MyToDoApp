import {appElements} from './globals.js';

class PopUp {
    constructor(popUpData, parentEl) {
        this.popUpData = popUpData;
        this.parentEl = appElements.appContent;
        this.blockScreen = appElements.blockScreen;
        this.popUpHTML = 
    }
}